mixin skill(item)
  li.skill= item.skill
    .skill__graph
      -for(let n = 0; n < item.max; n++)
        if n < item.value
          .skill__circle.skill__circle_blue
        else
          .skill__circle